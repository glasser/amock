package edu.mit.csail.pag.amock.subjects.callback;

import java.util.*;

public class MyComparator<T> implements Comparator<T> {
    public int compare(T a, T b) {
        // This is a terrible terrible comparator...
        return 0;
    }
}
